{
  "name": "@mantras-next/vectorstores",
  "version": "0.1.0",
  "description": "Mantras-Next向量存储包，提供向量数据库的集成",
  "main": "dist/index.js",
  "module": "dist/index.mjs",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup src/index.ts --format cjs,esm --dts",
    "dev": "tsup src/index.ts --format cjs,esm --dts --watch",
    "lint": "eslint \"src/**/*.ts\"",
    "test": "vitest run",
    "clean": "rimraf dist"
  },
  "keywords": [
    "ai",
    "agent",
    "ide",
    "mantras",
    "vectorstore",
    "embedding"
  ],
  "author": "Mantras Team",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.10.0",
    "eslint": "^8.54.0",
    "rimraf": "^5.0.5",
    "tsup": "^8.0.1",
    "typescript": "^5.3.2",
    "vitest": "^0.34.6"
  },
  "dependencies": {
    "@mantras-next/core": "workspace:*",
    "zod": "^3.22.4"
  }
}